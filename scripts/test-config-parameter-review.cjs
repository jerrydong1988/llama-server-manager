const assert = require('node:assert/strict')
const fs = require('node:fs')
const path = require('node:path')

const root = path.resolve(__dirname, '..')
const read = relative => fs.readFileSync(path.join(root, relative), 'utf8')

const configPage = read('src/components/ConfigPage.tsx')
const shared = read('src/components/ConfigPage/shared.tsx')
const sections = read('src/components/ConfigPage/sections.tsx')
const styles = read('src/index.css')
const search = read('src/components/ConfigPage/ParameterSearch.tsx')
const panel = read('src/components/ConfigPage/ConfigChangePanel.tsx')
const directory = read('src/components/ConfigPage/ConfigDirectory.tsx')
const workspace = read('src/components/ConfigPage/configWorkspace.ts')
const hook = read('src/components/ConfigPage/useEngineCompatibility.ts')
const server = read('src-tauri/src/commands/server.rs')
const types = read('src/store/types.ts')

const localGuardIndex = configPage.indexOf('if (!local)')
assert.ok(localGuardIndex > 0, 'ConfigPage must retain an explicit empty-selection guard')
assert.doesNotMatch(
  configPage.slice(localGuardIndex),
  /\buse(?:State|Effect|Memo|Callback|Ref|Context|Reducer|LayoutEffect|EngineCompatibility)\s*\(/,
  'React hooks must stay before the local-config early return (GitHub issue #5 / React #310)',
)

assert.doesNotMatch(configPage, /_matchedElements/, 'ConfigPage must not use the racy module-level search registry')
assert.doesNotMatch(shared, /_matchedElements/, 'shared controls must not use a module-level search registry')
assert.match(search, /event\.key === 'Enter'/, 'Enter must navigate parameter search results')
assert.match(search, /event\.shiftKey \? -1 : 1/, 'Shift+Enter must navigate backwards')
assert.match(search, /event\.key === 'Escape'/, 'Escape must clear parameter search')
assert.match(search, /querySelectorAll<HTMLElement>\('\[data-config-search-match="true"\]'/, 'search must collect rendered matches in DOM order')
assert.match(search, /primaryControl[\s\S]*button\[role="switch"\]/, 'search focus must prefer the editable control over the field help button')
assert.match(shared, /data-config-field=\{fieldKey\}/, 'fields must expose stable keys for locate actions')
assert.match(shared, /data-config-status="source"/, 'every field must expose exactly one source-state marker')
assert.match(shared, /data-config-source=\{source\}/, 'source-state markers must expose stable browser-test metadata')
assert.match(shared, /data-config-status="changed"/, 'unsaved edits must remain independently visible')
assert.match(shared, /data-config-emitted=\{emitted \? 'true' : undefined\}/, 'emitted fields must expose stable browser-test metadata')
assert.match(shared, /const FieldFrame =/, 'all parameter controls must share one aligned field frame')
assert.match(shared, /const ParameterHelp =/, 'parameter explanations must use the shared accessible help popover')
assert.match(shared, /parameterEngineDefault\(/, 'parameter help must expose the verified or probed engine default')
assert.match(shared, /label\.match\(\/-\{1,2\}/, 'uncatalogued fields must still derive command flags from their visible label')
assert.match(shared, /flags\.some\(flag => runtime\.unsupportedFlags\.has\(flag\)\)/, 'unsupported state must cover uncatalogued fields through their visible command flags')
assert.match(shared, /export const IntentNum =/, 'automatic numeric fields must expose inherited and manual modes directly')
assert.match(shared, /min-h-8 min-w-0 items-start justify-between/, 'field titles and status badges must occupy a stable header row')
assert.match(shared, /role="switch"[\s\S]*aria-label=\{label\}/, 'switch controls must retain an accessible field association after layout normalization')
assert.doesNotMatch(shared, /flex flex-wrap items-center gap-2\.5/, 'switch labels must not wrap independently from their controls')
assert.match(shared, /config-layout-container/, 'parameter sections must establish an inline-size layout container')
assert.match(sections, /config-form-grid/, 'parameter groups must use the shared responsive grid')
assert.match(styles, /@container \(min-width: 42rem\)[\s\S]*grid-template-columns: repeat\(2, minmax\(0, 1fr\)\)/, 'wide parameter containers must render exactly two aligned columns')
assert.doesNotMatch(sections, /2xl:grid-cols-3/, 'parameter grids must not derive a cramped third column from the viewport width')
assert.match(shared, /emittedKeys: emittedParams \|\| EMPTY_FIELD_KEYS/, 'section context must provide backend-confirmed emitted fields')
assert.doesNotMatch(shared, /data-config-status="changed"[\s\S]{0,300}emerald/, 'the changed marker must not restore green parameter semantics')
assert.doesNotMatch(shared, /active: fieldKey/, 'stable field identity must not reuse the former active/green semantic')
assert.doesNotMatch(directory, /changedCount|emittedCount|changedLabel|emittedLabel/, 'the parameter directory must stay a clean navigation list without status counters')
assert.doesNotMatch(directory, /<Badge/, 'the parameter directory must not stack status badges')
assert.match(workspace, /gpu_layers: \['gpu_layers', 'gpu_layers_auto'\]/, 'automatic GPU-layer aliases must collapse into one review field')
assert.match(workspace, /ctx_size: \['ctx_size', 'ctx_size_auto'\]/, 'automatic context aliases must collapse into one review field')
assert.match(workspace, /kv_unified_mode: \['kv_unified_mode', 'kv_unified'\]/, 'legacy KV aliases must collapse into one review field')
assert.match(workspace, /canonicalConfigFields/, 'review counts must deduplicate compatibility aliases')
assert.match(workspace, /restoreReviewField/, 'single-field undo must restore every compatibility alias')
assert.match(configPage, /restoreReviewField\(current, savedBaseline, key\)/, 'the review panel must use alias-aware undo')
assert.match(configPage, /reviewOverrideKeys\.flatMap\(reviewFieldKeys\)/, 'inherit-all must clear every compatibility alias behind each review field')
assert.match(configPage, /overrideKeys=\{reviewOverrideKeys\}/, 'launch-mode override counts must deduplicate compatibility aliases')

assert.match(configPage, /commandPreview\?\.emittedOverrideKeys/, 'emission counts must consume backend preview metadata')
assert.match(configPage, /emittedMarker: labels\.changeWillEmit/, 'field markers must use an explicit will-emit label')
assert.match(configPage, /<FieldRuntimeProvider/, 'parameter source and help metadata must be provided once for the full form')
assert.match(shared, /onInherit\(reviewFieldKeys\(fieldKey\)\)/, 'field help restore must clear every compatibility alias')
assert.match(sections, /inherited=\{!hasExplicitOverride\(local, 'threads'\)\}/, 'zero-valued automatic fields must use explicit intent instead of magic-number styling')
assert.match(sections, /--mmap \/ --no-mmap/, 'inverse command flags must be presented as a positive capability toggle')
assert.match(panel, /'changes' \| 'emitted'/, 'review panel must separate draft changes from emitted parameters')
assert.match(panel, /onLocate/, 'review rows must support locating their field')
assert.match(panel, /onUndo/, 'change rows must support a single-field undo')
assert.doesNotMatch(panel, /slice\(0, 8\)/, 'review panel must not hide changes after the eighth row')

assert.match(server, /fn emitted_override_keys\(/, 'backend must derive emission metadata from generated commands')
assert.match(server, /fn canonical_override_key\(/, 'backend emission metadata must collapse compatibility aliases')
assert.match(server, /emitted_override_keys:/, 'command preview response must serialize emission metadata')
assert.match(types, /emittedOverrideKeys: Array<keyof InstanceConfig>/, 'frontend IPC type must expose emission metadata')
assert.match(hook, /setCommandPreview\(result\)/, 'live compatibility preview must retain backend command metadata')
assert.match(hook, /reportedDefaultsVersion \?\? 0\) < 1/, 'previously probed engines must be refreshed once without downgrading future metadata versions')

console.log('Config parameter review and search navigation regression checks passed.')
