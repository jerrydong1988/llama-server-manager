# 实例路由统计推进清单

更新时间：2026-09-14。按阶段独立提交 PR，并提供 CI 测试包供用户验收。

## 第一步：按 Key 统计

- [x] PR #119 用户测试通过，已合入 `master`（`f2e0d3b`）。
- [x] 稳定 Key 身份、请求结果、输入／输出／缓存用量、完整性、日汇总与独立保留策略。

## 第二步：请求诊断与统计可信度

- [x] 结构化失败环节、错误代码和固定安全原因；不持久化上游错误正文。
- [x] 详情、诊断摘要复制、管理器／响应／上游请求 ID 关联。
- [x] 每页 50 条的稳定游标、精确 UTC 时间、结果及错误类型检索，突破最近 100 条限制。
- [x] 预检输入、预留输出、上下文窗口、超出量单独存储，拒绝不算生成消耗。
- [x] 各接口完整用量覆盖率、待写入采样、写入延迟、丢失／写入异常与异常退出的可能缺口提示。
- [x] 正常退出写入收尾标记；使用进程持有的文件锁区分存活采集器与异常结束。
- [x] 存储事务失败／重试／重复记录、取消、断流、超时、并发 Key、后台服务重启回归验证。
- [x] 本地 Qwen3.5-2B-Q4_K_M + llama.cpp b10951：Chat、Responses、Anthropic 的普通及流式响应共六种情况对账通过。
- [ ] 用户验收第二步 CI 测试包。

核心验收：打开上下文超限请求，能看到 `91,675 + 65,536 > 131,072`，超出 `26,139`，以及降低输出上限或缩短历史的建议；实际生成用量保持不适用。确认旧记录可以读取、第三页历史可以检索、复制的响应 ID 能定位请求。

真实引擎回归可使用隔离的本地服务，设置 `LSM_USAGE_TEST_ADDRESS=127.0.0.1:<port>` 后执行 `cargo test --manifest-path src-tauri/Cargo.toml --locked real_engine_usage_matches_delivered_protocol_counters -- --ignored --nocapture`。该测试须有已加载的生成模型，常规 CI 不启动模型。向量／重排序的真实引擎对账矩阵仍可继续扩展。

## 第三步：性能与容量分析（待开发）

- [ ] 首个有效输出、排队、总耗时 P50/P95，小时趋势、慢请求、并发占用及按 Key／模型／实例分析。
- [ ] 引入可合并的耗时分布；不从耗时总和还原 P95，不平均分时段 P95。
- [ ] 建立运行基线后再设告警，并评估统计采集开销。
- [ ] 首页核心指标：请求成功率、首个有效输出 P95、完整用量覆盖率。拒绝、取消和失败分列。

## 第四步：Key 使用管理（待开发）

- [ ] 日／月用量软预算与提醒。
- [ ] 每 Key 并发限制与公平排队，尊重实例真实槽位；沿用已有 RPM 限制。

## 后续：强制 Token 额度（待开发）

- [ ] 在计量可靠性满足要求后，设计开始预留、并发一致性、结束结算，以及取消／中断／部分／未知用量处理。

持续遵守：未知不当零；缓存与推理 Token 不重复计入总量；累计流式事件不相加；不保存提示词、回答正文或密钥。异常会话提示只说明可能缺口，不代表已知损失数量；统计不是正式计费凭据。
