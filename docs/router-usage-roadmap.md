# 实例路由统计推进清单

更新时间：2026-09-17。五步功能和发版前审计修复均已通过用户测试；PR #124 已合并（`c504741`）。本清单已完成，纳入 v2.9.47 发布。

## 第一步：按 Key 统计

- [x] PR #119 用户测试通过，已合入 `master`（`f2e0d3b`）。
- [x] 稳定 Key 身份、请求结果、输入／输出／缓存用量、完整性、日汇总与独立保留策略。

## 第二步：请求诊断与统计可信度

- [x] 结构化失败环节、错误代码和固定安全原因；不持久化上游错误正文。
- [x] 详情、诊断摘要复制、管理器／响应／上游请求 ID 关联。
- [x] 每页 50 条的稳定游标、精确 UTC 时间、结果及错误类型检索，突破最近 100 条限制。
- [x] 预检输入、预留输出、上下文窗口、超出量单独存储，拒绝不算生成消耗。
- [x] 各接口完整用量覆盖率、待写入采样、写入延迟、丢失／写入异常与异常退出的可能缺口提示。
- [x] 正常退出写入收尾标记；使用进程持有的文件锁区分存活采集器与异常结束。
- [x] 存储事务失败／重试／重复记录、取消、断流、超时、并发 Key、后台服务重启回归验证。
- [x] 本地 Qwen3.5-2B-Q4_K_M + llama.cpp b10951：Chat、Responses、Anthropic 的普通及流式响应共六种情况对账通过。
- [x] 用户测试第二步 CI 测试包，功能暂未发现问题；提出使用统计页面增加直观图表。
- [x] 使用现有日汇总增加请求／输入输出 Token 趋势、请求结果占比、完整性分布，以及可按 Key／模型／实例切换并点击筛选的用量排行。
- [x] 用户验收图表改进后的 CI 测试包；PR #120 已合入 `master`（`dd0eae9`）。

核心验收：打开上下文超限请求，能看到 `91,675 + 65,536 > 131,072`，超出 `26,139`，以及降低输出上限或缩短历史的建议；实际生成用量保持不适用。确认旧记录可以读取、第三页历史可以检索、复制的响应 ID 能定位请求。

真实引擎回归可使用隔离的本地服务，设置 `LSM_USAGE_TEST_ADDRESS=127.0.0.1:<port>` 后执行 `cargo test --manifest-path src-tauri/Cargo.toml --locked real_engine_usage_matches_delivered_protocol_counters -- --ignored --nocapture`。该测试须有已加载的生成模型，常规 CI 不启动模型。向量／重排序的真实引擎对账矩阵已在第五步补齐。

## 第三步：性能与容量分析（与第四步合并推进）

- [x] 首个有效输出、排队、总耗时 P50/P95，小时趋势、慢请求、并发占用及按 Key／模型／实例分析。
- [x] 引入可合并的耗时分布；不从耗时总和还原 P95，不平均分时段 P95。
- [x] 建立运行基线后再设告警，并评估统计采集开销。
- [x] 首页核心指标：请求成功率、首个有效输出 P95、完整用量覆盖率。拒绝、取消和失败分列。

## 第四步：Key 使用管理（与第三步合并推进）

- [x] 日／月用量软预算与提醒。
- [x] 每 Key 并发限制与可选的全局入场公平排队，尊重已知实例槽位；沿用已有 RPM 限制。
- [x] 用户验收第三、第四步合并迭代及界面调整的 CI 测试包；PR #121 已合入 `master`（`77cc372`）。

合并迭代设计与验收见 [性能与 Key 管理实施计划](router-performance-key-management-plan.md)。

## 当前修复：Windows 路由停止后的端口释放

- [x] 隔离复现：先启动路由，再启动模型进程，停止路由后端口仍被占用。原因为模型子进程继承监听句柄，延长两次点击的间隔不能可靠解决。
- [x] 后台与界面内路由统一使用不可继承的监听句柄；保留 IPv4／IPv6 和真实端口冲突检查。更新后的界面通过能力标记识别并升级同版本的旧后台服务。
- [x] 用户验收修复包，暂未发现问题；PR #122 已合入 `master`（`a4caf08`）。

## 第五步：强制 Token 额度（已验收）

- [x] 默认关闭的日／月硬额度，转发前持久化预留、跨线程／进程原子检查、完整响应幂等结算。
- [x] 取消／中断／失败／部分／未知用量保守占用，重启和清除历史统计不清空额度账。
- [x] 配置、额度已结算／占用展示、协议兼容的错误诊断、旧后台能力升级。
- [x] 真实生成、向量与重排序模型共 19 次成功请求对账，以及并发超额、取消、输出覆盖参数、计数豁免与重启验证。
- [x] Octop 等客户端省略输出上限的兼容修复：可配置默认值、跨协议别名归一化、软预算原样转发，以及 24 次真实模型调用补充验证。
- [x] 用户验收修复后的 PR #123 CI 测试包，功能正常，已合入 `master`（`c9636c3`）。

本轮契约与验收见 [强制额度实施计划](router-hard-token-quota-plan.md)。

## 发版前审计修复（已验收）

- [x] 修复全部已配置 Key 被禁用时错误放开匿名访问；覆盖推理、发现与诊断入口，并升级旧后台能力标记。
- [x] 参数基线对齐 llama.cpp v0.4.1 的 255 个条目，保留经过能力探测的旧引擎兼容分支。
- [x] 强制额度账本分批清理超过 365 天的已结束请求与无引用历史周期，保留待结算预留，不修改额度余额；界面增加存储占用信息。
- [x] 用户验收 PR #124 测试包；再审计及完整发布检查通过，真实推理与向量模型的 24 次调用对账通过。

持续遵守：未知不当零；缓存与推理 Token 不重复计入总量；累计流式事件不相加；不保存提示词、回答正文或密钥。异常会话提示只说明可能缺口，不代表已知损失数量；统计不是正式计费凭据。
