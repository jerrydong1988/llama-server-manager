export const enUS = {
  nav: { modelRepo: 'Model Repo', engine: 'Engines', instances: 'Instances', config: 'Config', downloads: 'Downloads', cluster: 'Cluster', proxy: 'Routing', perf: 'Performance', bigScreen: 'Big Screen', logs: 'Server Logs', dashboard: 'Dashboard', guide: 'Guide', up: 'up', down: 'down' },
  instance: {
    title: 'Server Instances', create: 'Create Instance', newInstance: 'New Instance',
    name: 'Instance Name', namePlaceholder: 'e.g. Llama-3-8B',
    selectModel: 'Select Model', selectModelPlaceholder: 'Click button to select a model',
    selectEngine: 'Select Engine', port: 'Port',
    running: 'Running', stopped: 'Stopped', error: 'Error',
    model: 'Model', engine: 'Engine',
    start: 'Start', stop: 'Stop', delete: 'Delete Instance',
    confirmDelete: 'Are you sure you want to delete this instance?',
    noInstances: 'No instances. Click "Create Instance" to add one.',
    uptime: 'Running for', genCommand: 'Cmd', genCommandTitle: 'Generated Command',
    copyClipboard: 'Copy to Clipboard', directStart: 'Start Directly',
    commandSecretWarning: 'The API key is hidden in the preview. Copying the full command includes the key; handle it securely.',
    configParams: 'Configure', openBrowser: 'Open in Browser', testConnection: 'Test Connection',
    sysPath: 'System PATH', cancelCreate: 'Cancel',
    autoStart: 'Auto-start when program launches',
  },
  modelRepo: {
    searchPlaceholder: 'Search models...', scan: 'Scan Models', addDir: 'Add Directory',
    downloadMS: 'Download Model', browseFiles: 'Browse Files',
    saveDir: 'Save Directory', saveDirPlaceholder: 'Default: models subdirectory in app data folder',
    repoIdPlaceholder: 'Repo ID, e.g. unsloth/Qwen3.6-35B-A3B-GGUF',
    hfRepoIdPlaceholder: 'Repo ID, e.g. ggml-org/Qwen2.5-0.5B-Instruct-GGUF',
    queryFailed: 'Query failed: ', notFound: 'No GGUF files found', found: 'Found',
    files: ' files', inputRepoId: 'Enter repo ID', querying: 'Querying...',
    networkError: 'Network error', selectFromRepo: 'Select from Repository',
    downloadBtn: '\u2B07 Download', pause: '\u23F8 Pause', resume: '\u25B6 Resume', retry: '\u21BB Retry',
    cancel: '\u2715 Cancel', done: '\u2713 Done', failed: '\u2717 Failed', cancelled: 'Cancelled',
    noModels: 'No models found. Click "Scan Models" or "Add Directory".',
    delete: 'Delete', deleteConfirm: 'Delete this model? This cannot be undone!',
    openFolder: 'Show in Folder',
    modelDirs: 'Added Model Directories:', remove: 'Remove', removeDirConfirm: 'Remove this directory from scan list? Files will not be deleted.',
    modelDirEmpty: 'No .gguf files found in',
    msTitle: 'Download Model',
    typeModel: '\uD83D\uDCC4Model', typeMmproj: '\uD83D\uDCF7Projector', typeImatrix: '\uD83D\uDCCAImatrix',
    typeModelShort: 'Model', typeMmprojShort: 'Projector',
    mmprojCount: 'MMProj',
  },
  engineMgr: {
    scan: 'Scan Engines', addDir: 'Add Engine Directory',
    noEngines: 'No engines found. Click "Add Engine Directory" to select a parent folder containing llama-server. Subdirectories will be auto-scanned.',
    defaultEngine: 'Default Engine', setDefault: 'Set as Default',
    remove: 'Remove', removeConfirm: 'Remove this engine from the list? Files will not be deleted.', removeDirConfirm: 'Remove this directory from scan list? Files will not be deleted.',
    removeDirInUseTitle: 'Engine in use', removeDirInUse: 'This scan directory contains engines referenced by {count} instance(s). Reassign those instances before removing it.',
    openFolder: 'Open Folder', sysPath: 'System PATH',
    dirs: 'Added Engine Directories (auto-scan subdirectories):', addDirTitle: 'Select a parent directory containing llama-server (subdirectories will be auto-scanned)',
  },
  configPage: {
    title: 'Instance Config', saved: '\u2713 Saved', saving: 'Saving...', save: 'Save Config',
    savedMsg: 'Config saved to', savedHint: 'Return to Instances and click Start to apply the new config.',
    noInstance: 'Click the \u2699\uFE0F button on an instance card in Instances to configure it.',
    basic: 'Basic', reasoning: 'Reasoning & Generation', generation: 'Generation', advancedSampling: 'Advanced Sampling',
    performance: 'Performance', advanced: 'Advanced', network: 'Server & Network',
    embeddingBanner: '\uD83D\uDCCA Embedding model detected \u2014 Embedding mode auto-enabled. Generation/sampling/chat params are disabled.',
    vectorCleanupTitle: 'Incompatible settings cleaned',
    vectorCleanupDescription: 'The model now uses a vector workload. These settings were restored to safe defaults.',
    vectorCleanupSpeculative: 'Speculative decoding',
    vectorCleanupGeneration: 'Generation and sampling',
    vectorCleanupChat: 'Chat behavior',
    vectorCleanupMultimodal: 'Multimodal',
    vectorCleanupCustom: 'Custom arguments',
    vectorCleanupRuntime: 'Other runtime settings',
    vectorCleanupItems: 'items',
    advSamplingOn: 'On', advSamplingOff: 'Off',
    subCompute: '[Compute]', subCache: '[Cache]', subContext: '[Context Window]',
    subRope: '[RoPE Scaling]', subYarn: '[YaRN Tuning]',
    subNetwork: '[Network]', subServer: '[Server Features]', subEmbedding: '[Embedding]',
    subMemLoad: '[Memory & Loading]', subKvCache: '[KV Cache]', subGpuMem: '[GPU Memory]',
    subSpecDecoding: 'Speculative Decoding', subExpert: 'Expert Settings',
    subCacheExt: 'Cache Extension', subRopeYarn: 'RoPE / YaRN Scaling',
    subServerExt: 'Server Extensions', subAdvCache: '[Draft Cache & Checks]',
    subGpuDevice: '[GPU Device]', subMultiModel: '[Multi-Model / Router]',
    subMedia: '[Multimodal & Media]',
    advSectionTitle: 'Advanced', advSectionReset: 'Reset All', subGroupReset: 'Reset',
    subAdvReasoning: 'Reasoning Config', subAdvModelAdapt: 'Model Adaptation', subAdvSampling: 'Advanced Sampling',
    subAdvSamplingExt: 'Sampling Extensions', subAdvSpec: 'Speculative Decoding',
    specDisabled: '\uD83D\uDD12 Speculative decoding is disabled. Select --spec-type in Reasoning settings first',
    subAdvRope: 'Context Scaling / RoPE \u00B7 YaRN', subAdvKvCache: 'KV Cache', subAdvContextMgmt: 'Context Management',
    subAdvHardware: 'Hardware', subAdvServer: 'Server Basics', subAdvServerExt: 'Server Extensions',
    subAdvMulti: 'Multi-Model / Expert',
    customArgs: 'Custom Arguments',
    customArgName: 'Parameter name',
    customArgValue: 'Value (optional)',
    customArgAdd: 'Add parameter',
    customArgEmpty: 'No custom arguments',
    // ── Basic ──
    modelPath: 'Model Path', modelPathTip: 'Path to GGUF model file. This is the primary model that llama-server will load and serve. Required parameter.',
    modelPathBtn: 'Select from model repository',
    alias: 'Alias', aliasTip: 'Safe model identifier exposed to API clients. In managed mode, an empty value is filled from the instance name and shown in both the page and startup command so responses never expose the full model path.',
    lora: 'LoRA Path', loraTip: 'Path to a LoRA (Low-Rank Adaptation) adapter file. LoRA adapters modify model behavior without retraining the full model — e.g. style transfer, domain specialization. [Scene] Load a single adapter for fine-tuned behavior. For multiple adapters with custom scales, use --lora-scaled.',
    mmproj: 'Multimodal Projector', mmprojTip: 'Path to a multimodal projector (mmproj) file. Selecting a primary model attempts a safe automatic match; use the button on the right to choose a projector manually.',
    mmprojPathBtn: 'Select a multimodal projector from the model repository',
    grammarFile: 'Grammar File', grammarFileTip: 'Path to a GBNF grammar file (*.gbnf) that constrains structured output such as a particular JSON shape, SQL, or code syntax. GBNF and JSON Schema are alternative constraint mechanisms; they do not need to be set together. A mismatched grammar can make generation impossible.',
    grammar: 'Inline Grammar', grammarTip: 'Inline GBNF grammar rules as text input. Same effect as grammar file but defined directly in the config. [Scene] Quick prototyping without creating a file. Only one of grammar/grammar_file takes effect.',
    chatTemplate: 'Chat Template', chatTemplateTip: 'Select a built-in chat template (chatml, llama3, mistral, etc.). Leave empty to auto-detect from model metadata. [Scene] Override when auto-detection picks wrong template or when using custom fine-tuned models.',
    chatTemplateFile: 'Template File', chatTemplateFileTip: 'Path to custom Jinja chat template file (*.jinja). [Scene] For fine-tuned models with custom prompt formats. Only one of template/template_file takes effect.',
    reasoningFormat: 'Reasoning Format', reasoningFormatTip: 'Controls how thinking/reasoning tags are handled. "none"=keep tags in content as-is, "deepseek"=extract to reasoning_content field (modern), "deepseek-legacy"=keep tags but also populate reasoning_content. [Scene] "deepseek" for API consumers expecting structured reasoning.',
    reasoningSwitch: 'Reasoning', reasoningTip: 'Controls how the chat template enables or suppresses reasoning output. "auto" defers to the template and model capability; "on" or "off" explicitly overrides it. This primarily affects request formatting, output, and token use; it does not make the model file itself load faster.',
    reasoningPreserve: 'Preserve Reasoning', reasoningPreserveTip: 'Preserve the reasoning trace across the full conversation history instead of only the latest assistant message. Default follows the chat template and only works with templates that support preserved reasoning.',
    jinja: 'Jinja Template', jinjaTip: 'Enable Jinja2 template engine for chat formatting. Required for some custom chat templates that use Jinja syntax. [Scene] Enable when using template files with advanced Jinja features (loops, conditionals).',
    skipChatParsing: 'Skip Chat Parsing', skipChatParsingTip: 'Bypass all chat template parsing — raw prompt passthrough. The model sees your input exactly as-is. [Scene] For debugging prompt format issues or using models without chat templates. [Trade-off] Disables structured conversation features.',
    reasoningEffort: 'Reasoning Effort', reasoningEffortTip: 'Passes low/medium/high as reasoning_effort to the chat template. It matters only when the template and model recognize that hint; it may change reasoning tokens, latency, and output, but higher does not universally mean better or necessarily slower. Inherit when unsure.',
    reasoningBudget: 'Reasoning Budget', reasoningBudgetTip: 'Token budget for reasoning: -1=unrestricted, 0=end reasoning immediately, and N>0=at most N tokens. When the budget is exhausted, budget_message is injected before reasoning ends.',
    reasoningBudgetMsg: 'Budget Message', reasoningBudgetMsgTip: 'Message injected before the end-of-thinking tag when reasoning budget is exhausted. [Scene] Prompt the model to conclude: "Please summarize your reasoning and provide the final answer."',
    // ── Generation ──
    nPredict: 'Tokens to Generate', nPredictTip: 'Maximum number of tokens the model will generate per response. -1=unlimited. [Scene] Short answers ~256, paragraphs ~1024, long essays ~4096. [Trade-off] Too high=waste; too low=truncation. Works with ignore_eos to create truly unlimited generation (⚠️ may never stop).',
    ignoreEos: 'Ignore EOS', ignoreEosTip: 'Removes EOS as a natural sampling stop condition and can help diagnose premature endings. With unlimited prediction it may still stop through reverse prompts, client cancellation, timeout, or other limits, but is more likely to occupy a slot for a long time; always provide an external stop condition.',
    jsonSchema: 'JSON Schema', jsonSchemaTip: 'Converts a JSON Schema into sampling constraints so candidates follow the requested structure. An invalid or over-constrained schema can leave no candidates, truncate, or fail; clients should still parse and validate the final response. Usually choose either this or a GBNF grammar.',
    jsonSchemaFile: 'JSON Schema File', jsonSchemaFileTip: 'Path to a JSON Schema file for structured output. Alternative to inline --json-schema. [Scene] Large or reusable schemas stored in files. [Principle] Only one of json_schema/json_schema_file takes effect.',
    temp: 'Temperature', tempTip: 'Scales the sampling probability distribution. Lower values are usually more focused and higher values more random; zero is not a cross-backend determinism guarantee. Start from the model publisher\'s recommendation or engine default, then compare with a fixed evaluation set.',
    topK: 'Top-K', topKTip: 'Keeps only the K highest-probability candidates at each step; 0 disables this filter. Smaller values are usually more focused and larger values retain more candidates. It interacts with Top-P, Min-P, and sampler order, so there is no universal best range.',
    topP: 'Top-P', topPTip: 'Nucleus sampling keeps the smallest high-probability candidate set whose cumulative probability reaches P. Lower values are usually more focused and higher values more diverse. It interacts with Top-K, Min-P, temperature, and sampler order; no single value is best for every model.',
    repeatPenalty: 'Repeat Penalty', repeatPenaltyTip: 'Adjusts probabilities for tokens seen inside the penalty window; 1 disables it. Raising it may reduce loops but can harm terminology, code, and fixed formats. Interpret it together with --repeat-last-n and validate it on the target model.',
    seed: 'Seed', seedTip: 'Random-number seed. -1 chooses a random seed each run; a fixed value helps A/B comparisons and regression tests. Exact token-for-token reproduction is most likely only when the model, build, backend, threading, and every sampling condition are unchanged.',
    minP: 'Min-P', minPTip: 'Filters candidates below a fraction of the highest-probability token; 0 disables it. Higher values usually retain fewer candidates, but do not inherently make output “safer”. Other samplers and their order also determine the final candidate set.',
    presencePenalty: 'Presence Penalty', presencePenaltyTip: 'Applies a one-time penalty to tokens already seen inside the penalty window. 0 disables it. It can discourage returning to the same words or topics; the effective history is controlled by --repeat-last-n, so it does not necessarily cover the whole conversation.',
    frequencyPenalty: 'Frequency Penalty', frequencyPenaltyTip: 'Penalizes tokens according to how often they occur inside the penalty window. 0 disables it. It can reduce word-level repetition, while high values may harm terminology, code, or fixed formats. The effective history is controlled by --repeat-last-n.',
    repeatLastN: 'Penalty Window', repeatLastNTip: 'How many recent tokens to consider for repeat/frequency/presence penalties. Default 64, -1=entire context. [Scene] Longer values catch repetition across wider spans. [Trade-off] Too long=may penalize legitimate recurring terms.',
    reversePrompt: 'Reverse Prompt', reversePromptTip: 'Pattern that triggers immediate stop of generation when output matches. [Scene] Stop at "User:" in role-play, or "}" for JSON completion. [Principle] Pattern is removed from output unless --special is set.',
    special: 'Special Tokens', specialTip: 'Allows output processing such as reverse-prompt matching to retain or expose textual representations of special tokens. It does not automatically add extra BOS/EOS tokens to every response. Most API use should inherit the engine default.',
    spmInfill: 'SPM Infill', spmInfillTip: 'Use Suffix/Prefix/Middle (SPM) pattern for code infill instead of Prefix/Suffix/Middle. [Scene] Required for some code-completion models trained with SPM format (e.g., certain Qwen-Coder variants).',
    backendSampling: 'Backend Sampling', backendSamplingTip: 'Attempts to execute sampling on the compute backend instead of forcing the CPU path. Benefit and compatibility depend on the llama.cpp build and backend. It is emitted only when capability probing confirms support; restore inheritance if it causes errors or result differences.',
    // ── Advanced Sampling ──
    mirostat: 'Mirostat Mode', mirostatTip: 'Mirostat adaptive sampling: 0=off, 1=v1, 2=v2. It adapts the distribution toward a target surprise value; temperature remains part of the sampler pipeline and should not be assumed to be ignored. Learning rate and target entropy matter only while Mirostat is enabled.',
    mirostatLr: 'Mirostat LR', mirostatLrTip: 'Mirostat learning rate eta controls how quickly the algorithm corrects toward its target surprise. Higher values respond faster and may fluctuate more. It is active only while Mirostat is enabled and has no universal mapping to “creativity”.',
    mirostatEnt: 'Mirostat Entropy', mirostatEntTip: 'Mirostat target entropy/surprise tau is the value the algorithm attempts to maintain, not an independent quality or creativity switch. It is active only while Mirostat is enabled and should be evaluated on the target model.',
    xtcProbability: 'XTC Probability', xtcProbabilityTip: 'Controls how often the XTC filter is applied; 0 disables it. When active it removes candidates according to the threshold rule and changes the distribution. Raising it makes that filtering more frequent but does not guarantee better output.',
    xtcThreshold: 'XTC Threshold', xtcThresholdTip: 'Probability threshold used by XTC when identifying candidates. It is active only when XTC probability is above zero. Threshold and probability jointly set filter strength; change one at a time and compare output.',
    dynatempRange: 'Dynatemp Range', dynatempRangeTip: 'Range in which dynamic temperature may move around the base temperature; 0 disables it. The actual value responds to entropy in the current probability distribution rather than oscillating periodically. Larger ranges make that response stronger.',
    dynatempExp: 'Dynatemp Exponent', dynatempExpTip: 'Controls the curve that maps distribution entropy into the dynamic-temperature range. One is the baseline linear exponent, and it is active only when the range is above zero. Evaluate direction and magnitude together with the range and model distribution.',
    typicalP: 'Typical-P', typicalPTip: 'Locally typical sampling filters candidates by the distance between token information content and the distribution\'s expected information content; 1 disables it. It is not a universal upgrade over Top-P and interacts with sampler order.',
    dryMultiplier: 'DRY Multiplier', dryMultiplierTip: 'DRY penalizes detected repeated sequences; 0 disables it. Higher values penalize more strongly and can affect legitimate repetition, code, or structured text. Tune it together with DRY base, allowed length, and window.',
    dryBase: 'DRY Base', dryBaseTip: 'Controls how the DRY penalty grows with repeated-sequence length. It is active only when the DRY multiplier is above zero. Before overriding the engine default, confirm that repetition is not caused by the prompt or another sampler.',
    dryAllowedLength: 'DRY Allowed Length', dryAllowedLengthTip: 'Length of a matching sequence allowed before DRY begins penalizing repetition; it is not limited to consecutive identical tokens. Lower values are more aggressive and can penalize legitimate phrases. It is active only when the DRY multiplier is above zero.',
    dryPenaltyLastN: 'DRY Penalty Window', dryPenaltyLastNTip: 'How many recent tokens DRY scans for repeated patterns. -1=entire context (default). [Scene] Larger values catch repetition across wider spans but increase computation.',
    drySeqBreaker: 'DRY Sequence Breaker', drySeqBreakerTip: 'Character(s) that reset DRY repeat detection. Default includes newline, colon, quote, asterisk. [Scene] Add custom breakers (e.g., "###") for structured output formats where repeats are expected between sections.',
    adaptiveTarget: 'Adaptive Target', adaptiveTargetTip: 'Adaptive-P selects tokens near this target probability; negative values disable it. It dynamically changes candidate selection rather than acting as a general randomness knob. Enable it before tuning decay and verify results on the target task.',
    adaptiveDecay: 'Adaptive Decay', adaptiveDecayTip: 'Exponential-moving-average decay used by Adaptive-P, active only when adaptive target is non-negative. Higher values retain a longer, smoother history; lower values track recent distributions more quickly. Compare with a fixed evaluation set rather than treating it as a standalone quality control.',
    topNSigma: 'Top-N Sigma', topNSigmaTip: 'Filters candidates using a statistical scale derived from the probability distribution; negative values disable it. Higher thresholds are generally stricter, but the effect changes with each distribution and has no universal recommended range.',
    logitBias: 'Logit Bias', logitBiasTip: 'Adjusts logits by token ID before sampling; positive values raise relative likelihood and negative values lower it. Token IDs, including EOS, vary by model vocabulary and must be verified for the selected model rather than copied from examples. Extreme bias can distort output or prevent normal stopping.',
    samplers: 'Samplers', samplersTip: 'Custom sampler chain. Overrides the default sampler order. Format: "sampler1;sampler2;...". [Scene] "temperature;top_k;min_p" for a minimal chain. [Principle] Samplers apply in sequence — later samplers see already-filtered candidates.',
    samplerSeq: 'Sampler Order', samplerSeqTip: 'Single-letter shorthand for sampler-chain order (for example, "edskypmxt"). Each letter maps to one sampling step. It controls filter and transform order; it is not a random seed and does not provide reproducibility by itself.',
    // ── Performance ──
    threads: 'Threads', threadsTip: 'CPU threads used during model inference. Inherited mode lets llama.cpp choose for the environment. The best manual value depends on CPU topology, GPU offload, and concurrent load; too many threads can cause contention, so compare measured throughput and latency.',
    threadsBatch: 'Batch Threads', threadsBatchTip: 'CPU threads used during prompt and batch processing; inherited mode follows the main thread strategy. Whether it should exceed the main count depends on CPU topology, GPU offload, and prompt length, so compare measured prompt-processing time.',
    threadsHttp: 'HTTP Threads', threadsHttpTip: 'HTTP server thread pool size. -1=auto (scales with CPU cores). [Scene] Increase for high-concurrency API serving. [Trade-off] More threads = more memory. Default auto is sufficient for most use cases.',
    gpuLayers: 'GPU Layers', gpuLayersTip: 'Requested number of model layers to offload to GPU. Inherited mode lets the engine decide. A large value usually means “as many as possible”, but 99 does not guarantee every layer for every model. Reduce it on VRAM pressure and confirm actual offload in the load log.',
    gpuLayersAuto: 'Auto GPU Layers', gpuLayersAutoTip: 'Inherited mode omits -ngl and lets current llama.cpp automatic layer selection and memory fitting decide. The result is a compatibility starting point, not guaranteed optimal for every throughput or VRAM target. Use manual mode only when exact control is needed.',
    batchSize: 'Batch Size', batchSizeTip: 'Logical maximum batch size for prompt processing. Increasing it may improve throughput for some workloads and also raises temporary memory demand; reduce it on OOM or latency problems. Benefit varies by model, backend, and micro-batch size.',
    ubatchSize: 'Micro Batch Size', ubatchSizeTip: 'Physical micro-batch limit used for prompt processing and normally no greater than the logical batch. Lower values generally reduce per-batch memory pressure and may reduce throughput; benefit from raising it depends on backend and hardware.',
    parallel: 'Parallel Slots', parallelTip: 'Number of server request slots; -1 lets the engine choose automatically. More slots can serve more concurrent requests while allocating more context/KV resources and changing per-request performance. Auto does not imply a fixed four-slot result.',
    contBatching: 'Continuous Batching', contBatchingTip: 'Allows new requests to join an in-progress batch for multi-request scheduling. Throughput and single-request latency changes depend on concurrency, slots, model, and backend; a single-user setup does not need to force an override.',
    cachePrompt: 'Prompt Cache', cachePromptTip: 'Allows the server to reuse matching prompt-prefix computation. Hits and benefit depend on requests, slots, and cache policy. Disable it when diagnosing cache-related behavior; the off state explicitly emits --no-cache-prompt.',
    cacheReuse: 'Cache Reuse', cacheReuseTip: 'Minimum matching chunk size required when attempting cache reuse through KV shifting; the default is 0. It matters only with prompt caching enabled. Smaller positive values usually attempt reuse more readily and may add management overhead, so measure hits and latency.',
    cacheRam: 'Cache RAM (MiB)', cacheRamTip: 'RAM budget used by the server to retain reusable prompt/context state. 0 disables it and -1 removes the application-side limit. It does not automatically move the entire active KV cache from GPU to RAM; behavior is tied to cache reuse, checkpoints, and idle-slot caching.',
    warmup: 'Warmup', warmupTip: 'Runs an empty pass after model load so some allocations and compute paths initialize early. This increases startup time and may reduce cold-start work on the first real request, but cannot guarantee identical first-token latency every time.',
    ctxAuto: 'Auto Context', ctxAutoTip: 'Inherited mode omits -c so llama.cpp decides from model metadata and any enabled memory-fitting policy. The result is not necessarily the model\'s advertised maximum window. Manual mode pins and emits the entered size.',
    ctxSize: 'Context Size', ctxSizeTip: 'Context-window size in tokens. Zero loads it from the model. Raising it usually increases KV-cache memory, with the exact relationship depending on layers, GQA/SWA, KV types, slots, and unified-cache policy. Going beyond trained/scaled capacity may also reduce quality.',
    keep: 'Keep Tokens', keepTip: 'Number of initial prompt tokens preserved when context window fills. 0=default, -1=all. [Scene] Keep system prompt intact during long conversations — set to system-prompt-length.',
    swaFull: 'SWA Full Cache', swaFullTip: 'Keeps a full-size cache for models that use Sliding Window Attention instead of shrinking it to the window. This increases KV memory use. Any task-quality benefit depends on model architecture and context use and is not guaranteed.',
    contextShift: 'Context Shift', contextShiftTip: 'When context fills, attempts to continue by removing or recomputing older content instead of stopping immediately. It is not infinite memory: shifted content is no longer fully retained. Some models or context modes do not support it and runtime logs may disable it.',
    ctxCheckpoints: 'Context Checkpoints', ctxCheckpointsTip: 'Max context checkpoints per slot. Default 32. [Principle] Checkpoints allow rolling back to earlier context states. [Trade-off] More checkpoints=more memory. Requires cache-ram > 0.',
    checkpointMinStep: 'Checkpoint Min Step', checkpointMinStepTip: 'Minimum token spacing between context checkpoints. Default 8192; 0=no minimum. [Scene] Lower=more granular rollback; Higher=less memory overhead.',
    // ── RoPE / YaRN ──
    ropeScaling: 'RoPE Scaling', ropeScalingTip: 'Selects a RoPE position-encoding scaling method. "none" disables scaling; "linear" and "yarn" use different extension strategies. No method is best for every model. Override metadata only when the model and its training support it; a mismatch can reduce both short- and long-context quality.',
    ropeScale: 'RoPE Scale', ropeScaleTip: 'RoPE context-scaling factor, nominally expanding positional range by N. Actual usable length and quality still depend on model training and scaling method; the number alone does not guarantee a lossless equal-factor context extension.',
    ropeFreqBase: 'RoPE Freq Base', ropeFreqBaseTip: 'RoPE base frequency used by NTK-aware scaling. Inherited mode reads model metadata. A manual value must match model training and the intended scaling strategy; simply raising theta does not guarantee better long-range ability.',
    ropeFreqScale: 'RoPE Freq Scale', ropeFreqScaleTip: 'RoPE frequency-scaling factor with a nominal context extension of 1/N. It uses the inverse direction of --rope-scale; normally choose one representation and follow the model publisher\'s guidance.',
    yarnExtFactor: 'YaRN Ext Factor', yarnExtFactorTip: 'YaRN interpolation/extrapolation mix factor. -1 inherits and 0 means full interpolation. It is not a context multiplier by itself; override it only when the model explicitly uses YaRN and matching parameters are known.',
    yarnAttnFactor: 'YaRN Attn Factor', yarnAttnFactorTip: 'YaRN attention-magnitude scaling factor; -1 inherits the model or engine value. A mismatch may affect both short- and long-context behavior, so normally follow model metadata or publisher guidance.',
    yarnBetaSlow: 'YaRN Beta Slow', yarnBetaSlowTip: 'YaRN slow-correction parameter. -1 uses the model or engine default. It matters only with YaRN scaling and should normally inherit model metadata unless the model publisher provides a specific recommended value.',
    yarnBetaFast: 'YaRN Beta Fast', yarnBetaFastTip: 'YaRN fast/low correction-dimension parameter; -1 inherits. Together with Beta Slow it sets correction boundaries across frequency bands and is not a general “fix local coherence” control.',
    yarnOrigCtx: 'YaRN Orig Context', yarnOrigCtxTip: 'Original context size the model was trained on. 0=auto-detect from model. [Scene] Set explicitly when auto-detection is wrong — critical for correct YaRN scaling math.',
    // ── Memory & Loading ──
    flashAttn: 'Flash Attn', flashAttnTip: 'Controls Flash Attention: "auto" lets the build and model decide, "on" forces an attempt, and "off" disables it. It may reduce attention memory use and improve speed, but benefit varies by backend, model, and context length. Forcing it on an unsupported path may fail.',
    moeCpu: 'MoE CPU Layers', moeCpuTip: 'Keeps the requested number of MoE layers on CPU, trading CPU RAM and memory bandwidth for lower GPU memory use. Speed impact depends on active experts, remaining GPU offload, and backend. It only applies to MoE models; verify the load log and benchmark after changing it.',
    cpuMoe: 'All MoE CPU', cpuMoeTip: 'Keeps all MoE expert weights on CPU instead of selecting the first N MoE layers. This usually reduces GPU memory needed for experts but may greatly increase CPU and memory-bandwidth pressure. Non-MoE models do not benefit.',
    mlock: 'Memory Lock', mlockTip: 'Asks the operating system to keep mapped model pages in physical memory to reduce paging-related latency spikes. It limits memory reclamation and is suitable only when RAM is ample and page locking is permitted; low-memory systems can become less stable.',
    noMmap: 'Memory Mapping (mmap)', noMmapTip: 'When enabled, llama.cpp may memory-map the model and let the OS page file data on demand, often improving startup and page sharing. Turning it off emits --no-mmap and uses regular reads, which can increase load-time RAM and I/O. Disable only for mapping, filesystem, or driver compatibility issues.',
    noRepack: 'Weight Repacking', noRepackTip: 'When enabled, llama.cpp may transform supported weights into a compute-friendly memory layout, trading extra load time and memory for possible inference gains. Turning it off emits --no-repack. The effect varies by model, quantization, and backend.',
    directIo: 'Direct I/O', directIoTip: 'Read model files without using the operating system page cache. [Scene] Dedicated high-speed storage or avoiding model data displacing other cached files. [Trade-off] Benefits depend on the OS and storage device; leave disabled for typical systems.',
    numa: 'NUMA', numaTip: 'NUMA strategy for multi-socket servers. distribute spreads work across nodes, isolate stays on the startup node, and numactl uses the CPU map supplied by numactl.',
    perf: 'Performance Timing', perfTip: 'Controls llama.cpp internal timing collection. Recent supported engines normally enable it by default; turning it off explicitly emits --no-perf. Timings help diagnose prompt processing and generation, while overhead depends on the build and workload and does not require a blanket production disable.',
    checkTensors: 'Check Tensors', checkTensorsTip: 'Validates tensor data for invalid values (NaN, Inf) before loading. [Scene] Enable when debugging model quality issues. [Trade-off] Slightly slower load time.',
    fit: 'Memory Fit', fitTip: 'Inherited mode omits --fit and follows the selected engine. On lets the engine adjust parameters that the user has not pinned to fit device memory; Off explicitly disables that fitting. It does not necessarily override every manual value, so confirm the result in the startup log.',
    fitTarget: 'Fit Target (MiB)', fitTargetTip: 'Target memory margin per device for --fit. Comma-separated values in MiB; single value broadcast to all devices. Default 1024. [Scene] Increase if you need extra VRAM headroom for other applications.',
    fitCtx: 'Fit Min Context', fitCtxTip: 'Minimum context size that --fit will not reduce below. Default 4096. [Scene] Set a floor to prevent --fit from shrinking context too aggressively.',
    loraInitNoApply: 'LoRA Init No Apply', loraInitNoApplyTip: 'Load LoRA adapter weights but don\'t apply them. [Scene] Pre-load adapters that will be applied later via POST /lora-adapters API at runtime for dynamic adapter switching.',
    loraScaled: 'LoRA Scaled', loraScaledTip: 'LoRA adapter with custom scaling factor. Format: "path:scale,path2:scale2". [Scene] Load multiple LoRA adapters with different strengths — e.g., "style_lora.gguf:0.5,domain_lora.gguf:0.8".',
    // ── KV Cache ──
    cacheTypeK: 'K Cache Type', cacheTypeKTip: 'Data type for KV-cache Key tensors. Lower precision usually reduces VRAM/RAM use, but speed depends on backend support and quality may change; fewer bits do not guarantee faster inference. Inherited mode keeps the model and engine compatible default.',
    cacheTypeV: 'V Cache Type', cacheTypeVTip: 'Data type for KV-cache Value tensors. Lower precision usually reduces VRAM/RAM, but quality and speed depend on the model and backend kernels. K and V sensitivity differs by model, so benchmark them independently instead of assuming a fixed precision relationship.',
    cacheTypeDraftK: 'Draft K Cache', cacheTypeDraftKTip: 'KV Key type for the speculative draft model. Lower precision can reduce draft-cache memory but may reduce proposal accuracy and acceptance rate. It is active only with draft-model speculative decoding.',
    cacheTypeDraftV: 'Draft V Cache', cacheTypeDraftVTip: 'KV Value type for the speculative draft model. Balance memory, backend kernel support, draft quality, and acceptance rate; it is active only with draft-model speculative decoding.',
    kvUnified: 'KV Unified', kvUnifiedTip: 'Default follows llama.cpp (enabled when slot count is automatic). On forces one KV buffer shared by all slots; Off forces separate slot buffers.',
    noKvOffload: 'KV GPU Offload', noKvOffloadTip: 'When enabled, the available GPU backend may store and compute KV cache on the GPU. Turning it off emits --no-kv-offload, usually saving VRAM while moving work to CPU/RAM. Performance impact varies widely with hardware, context, and backend and should be measured.',
    cacheIdleSlots: 'Idle Slot Cache', cacheIdleSlotsTip: 'Controls whether the server caches idle-slot state for later reuse. Enabling it may use more RAM; turning it off emits --no-cache-idle-slots. Exact behavior also depends on unified KV, cache RAM, and the engine version.',
    // ── Speculative Decoding ──
    draftModel: 'Draft Model', draftModelTip: 'Model used to propose candidates for speculative decoding before the target model verifies them. It accelerates only when proposal acceptance and draft cost are favorable and consumes additional memory. Choose a model compatible with the target tokenizer and speculation method.',
    draftGpu: 'Draft GPU Layers', draftGpuTip: 'Requested draft-model layers to offload to GPU. Inherited mode lets the engine choose. A large value means “as many as possible”, but 99 does not guarantee every layer for every draft model. Active only with speculative decoding.',
    draftTokens: 'Draft Max Tokens', draftTokensTip: 'Maximum number of draft tokens generated per speculation step. Default 3. [Scene] Higher values for aggressive speculation on easy tasks; lower for conservative on creative tasks. [Trade-off] More drafts=faster IF accurate, but wastes compute if rejected.',
    specDraftNMin: 'Draft Min Tokens', specDraftNMinTip: 'Minimum draft tokens per step. 0=default. [Scene] Set a floor to ensure speculation always has at least N candidates. [Trade-off] Too high wastes compute on hard predictions.',
    specType: 'Speculative Type', specTypeTip: 'Speculative decoding algorithm. Multiple types can be comma-separated (e.g. draft-mtp,ngram-cache). Options: draft-mtp (Multi-Token Prediction), draft-simple/draft-eagle3/draft-dflash (draft model), ngram-* (lookup-based).',
    specDraftPMin: 'Draft P Min', specDraftPMinTip: 'Minimum probability threshold used when proposing draft candidates; 0 adds no threshold. It filters proposals and does not mean the target model will accept a token. Higher values create fewer candidates, and any speed benefit depends on acceptance rate and draft cost.',
    specDraftPSplit: 'Draft P Split', specDraftPSplitTip: 'Split probability used by speculative decoding; recent engines default to 0.1. It interacts with speculation type, draft probabilities, and acceptance rate. Neither direction guarantees lower latency, so compare acceptance and end-to-end timing.',
    specDraftDevice: 'Draft Device', specDraftDeviceTip: 'Selects the device for the draft model using a name recognized by the current build. Empty inherits engine selection. A separate device changes memory placement and transfer cost; it does not by itself guarantee parallel speedup.',
    lookupCacheStatic: 'Lookup Cache Static', lookupCacheStaticTip: 'Pre-generated cache file read by lookup/ngram speculative decoding and not updated during generation. It supplies reusable ngram candidates rather than acting as a general startup cache, and applies only to the matching ngram speculation types.',
    lookupCacheDynamic: 'Lookup Cache Dynamic', lookupCacheDynamicTip: 'Lookup-decoding cache file updated during generation. Updating it is not model learning and does not guarantee progressively faster output; benefit depends on repeated patterns and workload, and it introduces file I/O.',
    specDefault: 'Spec Default Config', specDefaultTip: 'Asks llama.cpp to apply its built-in speculative-decoding preset. It is a convenient starting point, not a guarantee of optimal settings for this model, hardware, or latency goal. Review the final command when also overriding child parameters.',
    specDraftBackendSampling: 'Draft Backend Sampling', specDraftBackendSamplingTip: 'Allows draft-model sampling on a supported compute backend. Turning it off emits --no-spec-draft-backend-sampling. Benefit and compatibility vary by build and backend, and it is active only with speculative decoding.',
    specDraftThreads: 'Draft Threads', specDraftThreadsTip: 'Number of CPU threads for draft model generation. 0=auto (same as main threads). [Scene] Draft model is typically smaller — fewer threads may be sufficient.',
    specDraftThreadsBatch: 'Draft Batch Threads', specDraftThreadsBatchTip: 'Number of CPU threads for draft model batch processing. 0=auto (same as draft threads). [Scene] Draft batch processing is lighter — adjust if needed.',
    // ── GPU & Device ──
    device: 'Device', deviceTip: 'Target GPU device(s). Comma-separated list: "CUDA0,CUDA1". [Scene] Single GPU: leave empty (auto). Multi-GPU: explicitly list. "CPU"=no GPU acceleration.',
    splitMode: 'Split Mode', splitModeTip: 'Multi-GPU allocation mode: "none" avoids cross-device splitting, "layer" distributes layers, and "row" splits tensor rows. Available values come from current engine capability probing. Use it together with tensor-split and main-gpu.',
    tensorSplit: 'Tensor Split', tensorSplitTip: 'Fraction of model per GPU. "3,2"=GPU0 gets 3/5, GPU1 gets 2/5. [Scene] Balance split proportional to VRAM of each GPU. [Principle] Fractions sum determines total across GPUs.',
    mainGpu: 'Main GPU', mainGpuTip: 'Primary GPU index for single-GPU layers and intermediate results. 0=first GPU. [Scene] Designate the GPU with most VRAM as main. Only relevant with split_mode=row.',
    overrideKv: 'Override KV', overrideKvTip: 'Override model key-value metadata. Format: "key=type:value,...". [Scene] Fix incorrect metadata (e.g., "tokenizer.ggml.add_bos_token=bool:false") or experiment with different configs.',
    // ── Multi-Model ──
    modelsDir: 'Models Dir', modelsDirTip: 'Directory the router server scans for GGUF files and loads on request. Model identifiers and selection behavior are determined by the current engine and the request model field.',
    modelsPreset: 'Models Preset', modelsPresetTip: 'Path to the router server model-preset INI file. Its format and available rules evolve with llama.cpp; follow the selected engine examples and confirm successful loading in the startup log.',
    modelsMax: 'Models Max', modelsMaxTip: 'Max simultaneously loaded models in router mode. Default 4; 0=unlimited. [Trade-off] More models means more VRAM.',
    modelsAutoload: 'Models Autoload', modelsAutoloadTip: 'Controls whether the router server loads an unloaded requested model on demand. When disabled, models must be loaded through a method supported by the current engine before requests. Inherited mode follows the selected engine default.',
    // ── Multimodal ──
    mmprojUrl: 'MMProj URL', mmprojUrlTip: 'Remote URL for mmproj file. Auto-downloaded on first use and cached. [Scene] Convenient setup — just paste the URL instead of manually downloading the projector file.',
    mmprojAuto: 'MMProj Mode', mmprojAutoTip: 'Inherited mode lets the selected engine decide whether to use an available projector automatically. On explicitly enables automatic projector use; Off explicitly disables it. This does not replace a manually selected --mmproj file, and defaults vary by engine version.',
    noMmproj: 'Disable MMProj', noMmprojTip: 'Prevents automatic use or loading of an available multimodal projector for an explicitly text-only instance. Image input becomes unavailable. Memory savings depend on the specific projector and should not be estimated as a fixed amount.',
    noMmprojOffload: 'MMProj GPU Offload', noMmprojOffloadTip: 'Allows the multimodal projector to use the GPU backend. Turning it off emits --no-mmproj-offload, which can save some VRAM but may slow image encoding. Memory and speed effects depend on the projector and backend.',
    imageMinTokens: 'Image Min Tokens', imageMinTokensTip: 'Minimum per-image token constraint for multimodal preprocessing. Inherited mode uses projector/model metadata and engine policy. Raising it may preserve more image detail while consuming more context and memory.',
    imageMaxTokens: 'Image Max Tokens', imageMaxTokensTip: 'Maximum per-image token constraint for multimodal preprocessing. Inherited mode uses projector/model metadata. Lowering it can cap context and memory use but may remove image detail.',
    mtmdBatchMaxTokens: 'MTMD Batch Tokens', mtmdBatchMaxTokensTip: 'Max image tokens per batch for MTMD vision encoding. Default 1024. Larger=fewer batches but more memory.',
    mediaPath: 'Media Path', mediaPathTip: 'Directory for local media files (images, audio). Files accessible via file:// URLs in API requests using relative paths. [Scene] Organize a local media library for multimodal processing.',
    tags: 'Tags', tagsTip: 'Comma-separated model tags (informational). Used in multi-model mode for model identification and routing. [Scene] "coding,math" for a code-specialized model.',
    tools: 'Tools', toolsTip: 'Comma-separated built-in tools, for example "read_file,grep_search,exec_shell_command". Use "all" to enable every tool. Only enable tools in trusted environments.',
    // ── Server & Network ──
    host: 'Host', hostTip: 'IP address to bind. "127.0.0.1"=localhost only (secure); "0.0.0.0"=all network interfaces (LAN/internet accessible). [Scene] Use 0.0.0.0 for LAN sharing; 127.0.0.1 for local-only security.',
    portLabel: 'Port', portLabelTip: 'TCP port the server listens on. Default 8080. [Scene] Use different ports for multiple instances. [Note] Port conflicts are auto-detected before startup.',
    apiKey: 'API Key', apiKeyTip: 'API key for bearer token authentication. Multiple keys can be comma-separated. [Scene] Set when exposing the server to a network. [Principle] Clients must include "Authorization: Bearer YOUR_KEY" in requests.',
    apiKeyFile: 'API Key File', apiKeyFileTip: 'Read API key from file instead of inline. [Scene] More secure — avoids hardcoding keys in config files. Only one of key/key_file takes effect.',
    corsOrigins: 'CORS Origins', corsOriginsTip: 'Comma-separated allowed origins. Leave empty for the llama.cpp default (*); use localhost to reflect only local origins.',
    corsMethods: 'CORS Methods', corsMethodsTip: 'Comma-separated allowed HTTP methods. Leave empty for GET, POST, DELETE, OPTIONS.',
    corsHeaders: 'CORS Headers', corsHeadersTip: 'Comma-separated allowed request headers. Leave empty for the llama.cpp default (*).',
    corsCredentials: 'CORS Credentials', corsCredentialsTip: 'Allow credentials in CORS requests. Default follows llama.cpp (enabled). Restrict origins and configure an API key before exposing the server to a network.',
    noUi: 'Built-in WebUI', noUiTip: 'Controls whether llama-server serves its built-in WebUI at the root endpoint. Turning it off emits --no-ui. API-only deployments may disable it, but it is not a meaningful general performance or memory optimization.',
    offline: 'Offline Mode', offlineTip: 'Forces cache-only operation — blocks all network access. [Scene] Air-gapped environments or when you want to ensure no external calls. Requires pre-downloaded models and projectors.',
    pathPrefix: 'Static Files Directory', pathPrefixTip: 'Directory served as the static file root by --path. This is not a URL prefix; use --api-prefix for URL routing prefixes.',
    apiPrefix: 'API Prefix', apiPrefixTip: 'Path prefix from which server endpoints are served, without a trailing slash. Empty means no additional prefix. For example, /llama places APIs under /llama/.... Clients and health checks must use the same prefix.',
    uiConfigFile: 'UI Config File', uiConfigFileTip: 'JSON file with default WebUI settings (theme, layout, default model, etc.). [Scene] Pre-configure the WebUI for specific deployment needs.',
    uiConfig: 'UI Config (JSON)', uiConfigTip: 'Inline JSON string with default UI settings. Alternative to --ui-config-file. [Scene] Quick inline config without creating a file.',
    uiMcpProxy: 'MCP Proxy', uiMcpProxyTip: 'Enables MCP CORS proxy for the Web UI. [Scene] Allows the Web UI to access MCP servers from different origins. ⚠️ Do not enable in untrusted environments — bypasses CORS restrictions.',
    agent: 'Agent Mode', agentTip: 'Enables CORS proxy and all built-in tools. [Scene] Quick setup for AI Agent functionality (equivalent to --tools all + CORS proxy). ⚠️ Do not enable in untrusted environments.',
    sslKey: 'SSL Key', sslKeyTip: 'PEM-encoded SSL private key file. [Scene] Enable HTTPS on the server. Requires ssl_cert_file. [Principle] cert+key pair for TLS encryption.',
    sslCert: 'SSL Certificate', sslCertTip: 'PEM-encoded SSL certificate file. [Scene] Self-signed cert for internal use; CA-signed cert for public deployment. Use with ssl_key_file.',
    metrics: 'Metrics', metricsTip: 'Enables Prometheus-compatible /metrics endpoint for monitoring. [Scene] Production deployments — monitor request rates, token throughput, cache hit rates.',
    props: 'Dynamic Props', propsTip: 'Enables /props endpoint for runtime server property modification. [Scene] Change server parameters without restart. [Principle] POST to /props with JSON to update config live.',
    slotsEnabled: 'Slot Management', slotsEnabledTip: 'Controls availability of the slot-management endpoint. Turning it off explicitly emits --no-slots; when enabled or inherited, the current engine decides whether a flag is needed. The application manages this field for monitoring and diagnostics.',
    slotSavePath: 'Slot Save Path', slotSavePathTip: 'Directory in which slot save/restore endpoints may read and write KV-state files. Setting a directory does not automatically save every conversation or restore it after restart; clients must call the relevant slot endpoints and manage those files.',
    logPromptsDir: 'Prompt Log Directory', logPromptsDirTip: 'Write request prompts to this directory, creating it automatically. Debug use only: prompt logs may contain sensitive content.',
    slotPromptSimilarity: 'Slot Prompt Similarity', slotPromptSimilarityTip: 'Prompt-prefix similarity threshold required before a new request reuses a slot; 0 disables the similarity requirement. Raising it requires closer prompts, which may reduce incorrect reuse and also lower hit rate.',
    prefillAssistant: 'Prefill Assistant', prefillAssistantTip: 'Allows a final assistant message in the request to be treated as a prefix that generation continues, rather than a completed reply. It matters only when the request supplies such a prefix and the template supports it; the application does not inject fixed text automatically.',
    embedding: 'Embedding Mode', embeddingTip: 'Configures the instance for a vector workload, enabling embedding-related endpoints and disabling inapplicable chat, generation, and sampling fields. Available endpoints also depend on model workload and reranking mode, so it is not limited to one fixed URL.',
    pooling: 'Pooling', poolingTip: 'Strategy used by an embedding model to combine token representations. Empty reads the model default. mean, cls, last, and rank must match how the model was trained; mean is not a universal recommendation for every embedding model.',
    embdNormalize: 'Embed Normalize', embdNormalizeTip: 'Embedding normalization: -1=none, 0=max-absolute int16 scaling, 1=L1, 2=L2 (default), and values above 2 use p-norm.',
    reranking: 'Reranking', rerankingTip: 'Enables the /v1/rerank endpoint and rank-pooling semantics. Use only a model trained as a reranker; it does not turn an arbitrary embedding or generation model into an automatic retrieval-quality improvement.',
    timeout: 'Timeout (s)', timeoutTip: 'Network read/write timeout in seconds. It should cover the longest expected request without leaving lost connections indefinitely. It is not a precise total generation-token deadline, and active streaming can continue to refresh I/O.',
    sleepIdle: 'Idle Sleep (s)', sleepIdleTip: 'Auto-unload model after N idle seconds to free VRAM. -1=disabled. [Scene] Set 300-600 for shared GPU environments — model reloads on next request. [Trade-off] Saves VRAM but adds latency on wake.',
    verbose: 'Verbose Logs', verboseTip: 'Enables maximum verbosity logging (-v flag). [Scene] Debugging startup issues, model loading errors, or unusual behavior. [Trade-off] Much more log output — use temporarily.',
    rpcServers: 'RPC Servers', rpcServersTip: 'Comma-separated list of RPC server endpoints for distributed inference. [Scene] Offload model layers to remote GPU servers. [Principle] Each RPC server handles a portion of the model computation. Format: "host:port,host2:port2".',
    ssePingInterval: 'SSE Ping Interval', ssePingIntervalTip: 'Server-Sent Events ping interval in seconds. Default 30; -1 disables pings. Adjust it for proxies with different timeout settings.',
    reusePort: 'Reuse Port', reusePortTip: 'Allow multiple sockets to bind to the same port (SO_REUSEPORT). [Scene] Multi-process server deployments for load balancing. [Principle] Kernel distributes incoming connections across all listening sockets.',
    // ── Validation warnings ──
    warnA1: 'reasoning=off but reasoning format/effort/budget/preservation is set — these will be ignored',
    warnA2: 'Embedding mode is ON but generation/sampling params are set — they will be ignored by the server',
    warnA3: 'This speculative decoding mode requires an external draft model, but draft_model_path is empty',
    warnA3MtpNeedsDraft: 'Model metadata does not report built-in MTP heads, so draft-mtp needs an external draft model',
    warnA3MtpUnknown: 'Unable to confirm built-in MTP heads from model metadata; if startup fails, set an external draft model or rescan models',
    warnA4: 'Backend sampling is known to cause warnings on ROCm GPUs — consider disabling it',
    warnA5: 'Speculative type is empty/off but draft token counts are non-default — they will be ignored',
    warnA6: 'Auto context is enabled but RoPE/YaRN scaling params are set — they may not apply correctly',
    warnA7: 'Context size exceeds model context length by more than 4x — accuracy may degrade',
    warnA8: 'Image token limits set but no multimodal projector (mmproj) is provided',
    warnA9: 'This model family is known not to support SWA full cache; the server may disable it or fail to start',
    warnA10: 'Flash attention=on but the engine backend is CPU — flash attention requires a GPU',
    warnA11: 'Both grammar file and inline grammar are set — only one will be used',
    warnA12: 'Both chat template and template file are set — only one will be used',
    warnA13: 'Both API key and API key file are set — only one will be used',
    warnA14: 'TLS private key and certificate must be configured together',
    warnB1: 'Warmup is enabled together with embedding mode — warmup has no effect in embedding mode',
    warnB2: 'Cache RAM or cache reuse is set but prompt cache is disabled — they will be ignored',
    warnB3: 'Slot save/similarity is set but slots are disabled — they will be ignored',
    warnB4: 'Mirostat is enabled and temperature differs from default — mirostat overrides temperature',
    warnB5: 'Custom sampler chain is set alongside individual sampler params — the individual params may be overridden',
    warnB6: 'Pooling strategy is set but embedding mode is not enabled — pooling will be ignored',
    warnB7: 'GPU layers=0 but a GPU device is specified — no layers will be offloaded to the GPU',
    warnB8: 'Context checkpoints are set but cache RAM is 0/disabled — checkpoints require cache RAM',
    warnB9: 'Lookup cache files set but speculative type does not include an ngram type — they will be ignored',
    warnB10: '--no-mmproj is set but --mmproj path is also set — only one will apply',
    warnB11: 'Both inline JSON schema and JSON schema file are set — only one will be used',
    warnC1: 'A multimodal projector is set, but the model metadata explicitly identifies a text-only model; verify the model and projector',
    warnC1Unknown: 'The selected projector has no readable source metadata, so compatibility with the primary model cannot be verified automatically',
    warnC1Weak: 'The projector match is inferred only from the shared directory; verify that both files came from the same model repository',
    warnC1Mismatch: 'The projector and primary model report conflicting source metadata; select the projector distributed for this model',
    warnC4: 'Model already has built-in MTP heads — external draft model may be redundant. Ignore if intentional',
    warnC5: 'The primary model path points to a multimodal projector file; select the text/vision model file as model_path',
    warnC3: 'Unlimited generation (n_predict=-1) with ignore_eos=true — generation may never stop',
    warnD1: 'Custom args contain flags already supported in config UI — modify there to avoid conflicts',
  },
  perfBlock: {
    noRunning: 'No running instances. Start an instance to monitor performance.',
    waiting: 'Waiting for data...',
    noMetricsData: 'Collecting data... (needs a few polling cycles)',
    noSlots: 'No slot data available',
    cpu: 'CPU',
    memory: 'Memory',
    vram: 'VRAM',
    uptime: 'Uptime',
    tokensPerSec: 'Tokens/s',
    requests: 'Requests',
    slots: 'Slots',
    kvCache: 'KV Cache',
    kvCacheUsage: 'KV Cache Usage (%)',
    resources: 'CPU / Memory',
    slotStatus: 'Slot Status',
    busy: 'Busy',
    idle: 'Idle',
    perfTitle: 'Performance Analysis',
    waitingActivity: 'Waiting for inference activity...',
    task: 'Task',
    slot: 'Slot',
    generated: 'Generated',
    tokens: 'tokens',
    specAccept: 'Spec Accept',
    lastCompleted: 'Last Completed',
    prompt: 'Prompt',
    promptSpeed: 'Prompt Speed',
    genSpeed: 'Gen Speed',
    total: 'Total',
    totalTime: 'Total Time',
    specGenTime: 'Spec Gen Time',
    speedCurve: 'Speed Curve (t/s)',
    waitingMetrics: 'Waiting for metrics...',
    procLabel: 'Proc',
    sysLabel: 'Sys',
    searchParams: 'Search params...',
    gpu: 'GPU',
  },
  logs: {
    title: 'Server Logs', selectInstance: 'View instance logs:', allInstances: 'All Instances',
    clear: 'Clear Logs', clearConfirm: 'Clear all logs for this instance?', clearAllConfirm: 'Clear logs for ALL instances?', noLogs: 'No log output',
    runningTag: '(running)', noInstances: 'No running instances. Create and start an instance first.',
    noLogsForInstance: 'No logs for this instance',
    hint: 'Logs are displayed in real-time. Start an instance to capture output. Select an instance to view grouped logs.',
paused: 'Paused', scrollToBottom: 'Latest', following: 'Live', entries: 'entries',
  },

  clusterPage: {
    title: 'Cluster Management',
    workerList: 'Worker List',
    online: 'Online',
    offline: 'Offline',
    testing: 'Testing',
    unknown: 'Unknown',
    scanLan: 'Scan LAN',
    scanning: 'Scanning...',
    addWorker: 'Add Worker',
    editWorker: 'Edit Worker',
    deleteWorker: 'Delete Worker',
    copyLaunchCmd: 'Copy Launch Command',
    noWorkers: 'No workers found. Ensure rpc-server is running on worker machines.',
    totalVRAM: 'Total Available VRAM',
    selectedWorkers: 'Selected Workers',
    testConnection: 'Test Connection',
    refreshDevices: 'Refresh Devices',
    deviceType: 'Device Type',
    deviceName: 'Device Name',
    vramUsage: 'VRAM Usage',
    noDevices: 'Unable to retrieve device information',
    networkSettings: 'Network Settings',
    usb4Detect: 'Detect USB4 Adapters',
    usb4None: 'No USB4 adapters detected',
    configureIp: 'Configure IP',
    ipAddress: 'IP Address',
    subnetMask: 'Subnet Mask',
    workerSelector: 'Cluster Workers',
    manualInput: 'Manual Input',
    switchToManual: 'Switch to Manual Mode',
    switchToVisual: 'Switch to Visual Mode',
    cmdPreview: 'Command Preview',
    oneClickLaunch: 'One-Click Launch Worker',
    launchWizard: 'Launch Wizard',
    sshConfig: 'SSH Configuration',
    sshUser: 'SSH Username',
    sshKeyPath: 'SSH Key Path',
    rpcPort: 'RPC Port',
    launchSuccess: 'Worker launched successfully',
    launchFailed: 'Worker launch failed',
    discoverMode: 'Auto Discovery',
    syncFromCluster: 'Sync from Cluster Page',
    clusterThroughput: 'Cluster Throughput',
    confirmDelete: 'Are you sure you want to delete this worker?',
    ipFormatError: 'Invalid IP address format',
    stopScan: 'Stop Scan',
    localLaunch: 'Local Launch',
    localWorker: 'Local',
    stopLocalWorker: 'Stop Local Worker',
    launchErrorDefault: 'Launch failed, no error details',
    sshWarning: '⚠ Ensure remote rpc-server version matches the main engine. It is recommended to upload the rpc-server binary from the main engine directory to the remote.',
    sshPort: 'SSH Port',
    remoteOS: 'Remote OS',
    autoDetect: 'Auto Detect',
    remoteRpcPath: 'Remote rpc-server path (leave empty for PATH default)',
    rpcPathPlaceholder: 'rpc-server (PATH default)',
    prevStep: '← Previous',
    nextStep: 'Next',
    launching: 'Launching...',
    launchWorker: 'Launch Worker',
    localLaunchTitle: 'Local rpc-server Launch',
    engineDir: 'Engine Directory',
    engineMode: 'Engine',
    customMode: 'Custom',
    defaultEngineLabel: ' (default)',
    engineDirPlaceholder: 'Enter engine directory path...',
    selectEngineDir: 'Select Engine Directory',
  },

  dashboard: {
    title: 'Dashboard',
    runningInstances: 'Running',
    totalModels: 'Models',
    totalEngines: 'Engines',
    totalInstances: 'Instances',
    noRunning: 'No running instances',
    goToInstances: 'Go to Instances',
    stoppedInstances: 'Stopped Instances',
  },

  downloadPage: {
    title: 'Download Manager',
    sourceLabel: 'Source',
    repoLabel: 'Repo ID',
    saveDirLabel: 'Save Directory',
    browse: 'Browse',
    downloadAll: 'Download All',
    queue: 'Queue',
    queueCount: 'waiting',
    active: 'Active',
    paused: 'Paused',
    failed: 'Failed',
    completed: 'Completed',
    clearCompleted: 'Clear completed',
    clearFailed: 'Clear failed',
    noActive: 'No active downloads',
    noCompleted: 'No completed downloads',
    noPaused: 'No paused downloads',
    noFailed: 'No failed downloads',
    noQueue: 'No queued downloads',
    resumed: 'Resume',
    queued: 'Queued...',
    cancel: 'Cancel',
    retry: 'Retry',
    redownload: 'Re-download',
    pauseAll: 'Pause all',
    resumeAll: 'Resume all',
    cancelAll: 'Cancel all',
    files: 'files',
    speed: 'Speed',
    eta: 'Remaining',
    batchActions: 'Batch Operations',
    dlSettings: 'Download Settings',
    resumePolicy: 'Resume Policy',
    resumeManual: 'Manual resume',
    resumeAuto: 'Auto resume on launch',
    maxConcurrent: 'Max Concurrent',
    remoteChangedTip: 'Remote file changed, restarting download',
    loading: 'Loading...',
  },

  instanceWorkspace: {
    instances: 'instances', running: 'running', runningTitle: 'Running', stoppedTitle: 'Stopped',
    errored: 'Errored', autoStart: 'Auto Start', all: 'All', offline: 'Offline', healthy: 'Healthy',
    pending: 'Pending', checkingPort: 'Checking port...', portAvailable: 'Port is available',
    portInUse: 'Port is already in use', noEnginePrefix: 'No engine detected yet. Please open',
    noEngineSuffix: 'and add a llama-server installation.',
    searchPlaceholder: 'Search instances, model, engine, port...', allEngines: 'All Engines',
    name: 'Name', status: 'Status', health: 'Health', uptime: 'Uptime', actions: 'Actions',
    rename: 'Rename', moveUp: 'Move up', moveDown: 'Move down', checking: 'Checking...', more: 'More',
    listTitle: 'Instance List', details: 'Instance Details',
    noSelection: 'Select an instance to inspect runtime details and quick actions', endpoint: 'Endpoint',
    modelPath: 'Model Path', engine: 'Engine', primaryActions: 'Primary Actions', quickActions: 'Quick Actions',
    order: 'Order', autoStartHint: 'Start this instance automatically when the app starts', selected: 'Selected',
    operationTarget: 'Current Operation Target', operationTargetHint: 'Right-side actions apply to this instance',
    configuredEngineMissing: 'Configured engine is missing',
    missingEngineBanner: '{count} instance(s) reference an engine that no longer exists. The app will not silently substitute the default engine; select a replacement before starting or generating a command.',
    commandErrorTitle: 'Unable to generate startup command',
    missingEngineCommandError: 'The llama-server engine saved by this instance was removed or is no longer available. To avoid backend and parameter capability mismatches, the app will not switch engines automatically. Select an engine first.',
    noEngineCommandError: 'No available llama-server engine was found. Add or scan an engine, then select it for this instance before generating a command.',
    commandGenerationFailed: 'Command generation failed: {error}', reselectEngine: 'Select another engine',
  },

  modelRepoWorkspace: {
    sourceCount: '{count} source(s)',
    description: 'Curate local model assets, keep scan roots clean, and inspect the repo as one operational surface.',
    capacity: 'Capacity', scanRoots: 'Scan Roots', scanRootsDescription: 'Local directories included in repository scans.',
    explorer: 'Repository Explorer', explorerDescription: 'Search by file name, architecture, quantization, or artifact type.',
    emptyDescription: 'Add one or more model directories, then run a scan to populate the repository explorer.',
    assetDetails: 'Asset Details', assetDetailsDescription: 'Inspect the currently selected file and jump to its location.',
    noAssetSelected: 'Select a model artifact from the explorer to inspect it here.',
    type: 'Type', quant: 'Quant', architecture: 'Architecture', size: 'Size', shard: 'Shard', yes: 'Yes', no: 'No',
  },

  downloadWorkspace: {
    strategyTitle: 'Download strategy',
    strategySub: 'Resume policy, concurrency, bandwidth limit, and low-priority mode are backend-backed',
    addTaskTitle: 'Add download task', addTaskSub: 'Choose a model source and browse repository files',
    sourceSection: 'Model source', sourceHelp: 'Enter a repository ID, then browse downloadable files',
    storageSection: 'Download storage path',
    storageHelp: 'Use an absolute folder, or a relative folder such as models under the app data directory',
    storagePreview: 'Save target', chooseFolder: 'Choose folder', batchTools: 'Bulk tools',
    totalSpeed: 'Current speed', controllable: 'Controllable', strategy: 'Strategy',
    backendSaved: 'Backend connected', localOnly: 'Local', manualShort: 'Manual', autoShort: 'Auto on launch',
    concurrencyShort: 'Concurrency', bandwidth: 'Global bandwidth limit', displayUnit: 'Display unit',
    unlimited: 'Unlimited', limitHelp: '0 means unlimited', lowPriorityThrottle: 'Low-priority throttling',
    throttleHelp: 'Backend switches downloads to single concurrency; without a custom limit it applies a 2 MiB/s background cap',
    resetDefaults: 'Reset defaults', noActionable: 'No actionable downloads', localPreset: 'Local preset',
    taskSummary: 'Task details', noSelectedTask: 'No task selected', selectedTaskHint: 'Click any task card to switch this panel',
    progress: 'Progress', downloaded: 'Downloaded', speed: 'Speed', eta: 'ETA', etaPending: 'Pending',
    queueState: 'Queue state', queuePosition: 'Queue position', runningNow: 'Downloading now',
    notQueued: 'Not waiting in queue', failedReason: 'Failure reason', retryAdvice: 'Retry advice',
    retryAdviceNetwork: 'Keep the partial file and retry; if it fails again, check network or bandwidth settings',
    retryAdviceRedownload: 'The server or partial file state is not suitable for resume; use redownload',
    retryAdvicePermission: 'Check repository access and whether the file still exists, then retry',
    retryAdviceGeneric: 'Retry first to resume from the partial file; redownload if the error repeats',
    openLocation: 'Open location', addToModelRepo: 'Scan into model repo',
    importedToRepo: 'Scanned the save directory and refreshed the model repository',
    importFailed: 'Model repo scan failed', localPath: 'Local file',
    settingsLoadFailed: 'Failed to load download settings', settingsSaveFailed: 'Failed to save download settings',
    pausing: 'Pausing', batches: 'batches', resumable: 'resumable', moreFiles: '{count} more files',
  },

  appShell: {
    pageDescriptions: {
      dashboard: 'System overview and current llama-server activity',
      modelRepo: 'Browse local and remote model assets', downloads: 'Model transfer queue and active tasks',
      engine: 'Runtime binaries and execution backends', instances: 'Server instances, ports, and process state',
      config: 'Application defaults and runtime preferences', cluster: 'Distributed workers and network availability',
      proxy: 'Unified API endpoint and model alias routing', perf: 'Performance telemetry and resource signals',
      logs: 'Runtime logs and diagnostic output', guide: 'Reference material and operating notes',
      fallback: 'Manage local llama-server runtime state',
    },
    switchToLight: 'Switch to light mode', switchToDark: 'Switch to dark mode', languageLabel: '\u4E2D\u6587',
    languageTitle: '\u5207\u6362\u4E3A\u4E2D\u6587', commandCenter: 'Command Center', attentionCenter: 'Attention Center',
    runtimeWarnings: 'Runtime warnings', clear: 'Clear', close: 'Close',
    crash: { title: 'Something went wrong', description: 'The current view crashed and can be reloaded.', unknown: 'Unknown error', reload: 'Reload view' },
    proxyRunning: 'Background instances or routing are active',
    proxyExitDescription: 'Independent background mode is disabled. Enable and verify background handoff before exiting, keep the app in the tray temporarily, or explicitly stop all instances and routing.',
    keepInTray: 'Keep running in tray',
    openProxySettings: 'Open routing settings', stopProxyAndQuit: 'Stop instances, routing, and exit',
    enableBackgroundAndExit: 'Enable background and exit', backgroundDetachPreparing: 'Verifying background handoff…',
    backgroundDetachFailed: 'Background handoff failed',
    backgroundDetachFailureDescription: 'The main app remains open and continues managing current instances and routing. Review the error and retry.',
    backgroundDetachUnknownError: 'The background runtime returned no recognizable error details.',
    retryBackgroundDetach: 'Verify again and exit', cancelExit: 'Cancel exit',
  },

  commandCenter: {
    title: 'Command Center', needAttention: 'need attention', healthy: 'Healthy',
    description: 'Handle setup gaps, runtime issues, and frequent workflows from one place.',
    close: 'Close command center', search: 'Search pages, tasks, or actions...', commonTasks: 'Common Tasks',
    attention: 'Attention Center', noIssues: 'No setup or runtime issues need immediate action.',
    severityCritical: 'Critical', severityWarning: 'Warning', severityInfo: 'Info',
    noEnginesTitle: 'No runtime engines registered',
    noEnginesDescription: 'Scan or add llama-server binaries before starting instances.', openEngines: 'Open engines',
    noModelsTitle: 'Model inventory is empty',
    noModelsDescription: 'Add a local model folder or download models to make instance setup smoother.', openModels: 'Open models',
    noInstancesTitle: 'No server instances',
    noInstancesDescription: 'Create the first instance to manage ports, models, engines, and runtime state.', createInstance: 'Create instance',
    unhealthyTitle: 'Instance health needs attention', reviewInstances: 'Review instances',
    unhealthyDescription: '{errored} errored instance(s), {unhealthy} running health check failure(s).',
    failedDownloadsTitle: 'Failed downloads detected', failedDownloadsDescription: '{count} download task(s) need retry, cleanup, or requeue.',
    openDownloads: 'Open downloads',
    actions: {
      dashboardTitle: 'Open system cockpit', dashboardDescription: 'Review resources, instances, downloads, and key actions.',
      instancesTitle: 'Manage server instances', instancesDescription: 'Start, stop, open endpoints, and adjust instance configuration.',
      modelsTitle: 'Organize model inventory', modelsDescription: 'Scan local models and review shards and folders.',
      downloadsTitle: 'Manage download queue', downloadsDescription: 'Resume, pause, retry model downloads, and adjust transfer policy.',
      enginesTitle: 'Check runtime engines', enginesDescription: 'Scan binaries, set defaults, and verify execution backend.',
      performanceTitle: 'Open performance analysis', performanceDescription: 'Inspect telemetry, bottlenecks, instance throughput, and diagnostics.',
      proxyTitle: 'Manage instance routing', proxyDescription: 'Configure the unified OpenAI-compatible endpoint and route requests by model name.',
      logsTitle: 'Review runtime logs', logsDescription: 'Trace startup failures, download errors, and health check clues.',
      refreshTitle: 'Refresh all state', refreshDescription: 'Reload instances, models, engines, and queue state.',
      startDescription: 'Quickly start the first stopped instance.', stopDescription: 'Quickly stop the first running instance.',
      groupNavigation: 'Navigation', groupRuntime: 'Runtime', groupResources: 'Resources', groupSetup: 'Setup',
      groupDiagnostics: 'Diagnostics', groupServices: 'Services', groupMaintenance: 'Maintenance', startPrefix: 'Start', stopPrefix: 'Stop',
    },
  },

  common: {
    cancel: 'Cancel', confirm: 'Confirm', save: 'Save', delete: 'Delete',
    browse: 'Browse', download: 'Download', cancelDownload: 'Cancel Download',
    browseFiles: 'Browse Files', selectFromRepo: 'Select from Repository',
    default: 'Default', copySuccess: '\u2713 Copied!',
    appTitle: 'Llama Server Manager',
    updateAvailable: 'New Version', clickToDownload: 'Click to Download',
    autoStart: 'Auto Start',
    loading: 'Loading...',
  }
}
