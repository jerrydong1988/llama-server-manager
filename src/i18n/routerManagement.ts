export function getRouterManagementLabels(lang: string) {
  const zh = lang === 'zh-CN'
  return {
    hardQuota: zh ? '强制 Token 额度' : 'Hard token quotas',
    dayLimit: zh ? '每日 Token 硬额度' : 'Daily token hard limit',
    monthLimit: zh ? '每月 Token 硬额度' : 'Monthly token hard limit',
    quotaHint: zh ? '默认关闭，0 表示不限额。启用后按 UTC 日／月独立记账，不追溯历史；生成请求须明确输出上限。转发前预留额度，不足时拒绝；向量批量保守预留。中断或未知用量保留占用，清除统计、关闭再开启均不重置额度账。' : 'Off by default; zero means unlimited. Accounting starts when enabled, by UTC day/month, without historical backfill. Generation requires an explicit output limit. Requests reserve quota before forwarding and are rejected if insufficient; vector batches reserve conservatively. Interrupted/unknown usage retains its reservation. Clearing statistics or toggling limits does not reset the ledger.',
    quotaAccounting: zh ? '额度账' : 'Quota ledger',
    settled: zh ? '已结算' : 'Settled',
    held: zh ? '待结算／保守占用' : 'Pending / retained',
    remaining: zh ? '可预留' : 'Available',
    unlimited: zh ? '不限额' : 'Unlimited',
    quotaNote: zh ? '额度账按预留时的 UTC 日／月归属，仅记录启用硬额度后的调用，与上方实际用量独立。占用包含在途请求、异常退出遗留及未知用量；不会自动当作零退款，周期结束后不占用新周期。' : 'The ledger uses the UTC day/month of reservation and records only quota-enabled calls, separately from measured usage above. Held tokens include in-flight requests, crash leftovers and unknown usage; they are never automatically refunded as zero and do not carry into a new period.',
    performance: zh ? '性能与等待' : 'Performance and waiting',
    performanceHint: zh ? '总耗时与首个有效输出统计成功业务调用；排队包含等待超时。首个输出从请求进入路由时计时，包含排队。' : 'Duration and first output cover successful inference calls; queue time includes timeouts. First output includes admission waiting.',
    precision: zh ? '近似分位数，向上误差不超过约 6.25%。未采样显示 —；旧版本数据不补算分布。日分布保留 365 天，小时分布保留 30 天。' : 'Approximate percentiles, rounded up by at most about 6.25%. Missing samples show —; older data is not backfilled. Daily distributions: 365 days; hourly: 30 days.',
    duration: zh ? '总耗时' : 'Total duration', queue: zh ? '排队等待' : 'Queue wait', firstOutput: zh ? '首个有效输出' : 'First meaningful output',
    samples: zh ? '样本数' : 'Samples', metric: zh ? '耗时指标' : 'Latency metric',
    distribution: zh ? '分组性能' : 'Grouped performance', keys: zh ? 'API Key' : 'API Key', models: zh ? '模型' : 'Model', instances: zh ? '实例' : 'Instance',
    baseline: zh ? '基线：所选开始日期之前 7 个完整 UTC 日。当前与基线各至少 20 个样本，且 P95 超过基线 2 倍时提示。' : 'Baseline: 7 complete UTC days before the selected start date. Alerts need at least 20 samples in each period and P95 above twice the baseline.',
    elevated: zh ? 'P95 高于基线 2 倍' : 'P95 above twice the baseline',
    warming: zh ? '基线样本积累中' : 'Collecting baseline samples',
    normal: zh ? '未达到基线提示条件' : 'Baseline alert threshold not reached',
    empty: zh ? '当前筛选尚无性能样本' : 'No performance samples for these filters',
    loading: zh ? '正在读取…' : 'Loading…', hourly: zh ? '按小时' : 'Hourly', sixHourly: zh ? '每 6 小时' : 'Every 6 hours', daily: zh ? '按日' : 'Daily',
    current: zh ? '当前用量与并发' : 'Current usage and concurrency',
    currentPeriod: zh ? '当前 UTC 日／月 · 不受历史筛选影响' : 'Current UTC day / month · independent of history filters',
    concurrency: zh ? '实时并发' : 'Live concurrency', globalLimit: zh ? '全局上限' : 'Global limit',
    live: zh ? '路由运行中' : 'Router is running', unavailable: zh ? '状态暂不可用' : 'Status unavailable',
    inactiveHint: zh ? '启动路由后显示实时并发，已采集的用量仍可查看。' : 'Start the router to see live concurrency. Recorded usage remains available.',
    softBudgetNote: zh ? '软预算仅提醒，不会拒绝请求；可在路由配置中设置。' : 'Soft budgets only notify, never reject requests. Set them in routing settings.',
    budget: zh ? '预算' : 'Budget', keyDisabled: zh ? '已停用' : 'Disabled', incompleteLabel: zh ? '用量不完整' : 'Incomplete usage',
    currentHint: zh ? '下列用量固定为当前 UTC 日／月，不受上方历史筛选影响。软预算仅提醒，不会拒绝请求；在路由配置中设置。' : 'Usage below always covers the current UTC day/month, independent of historical filters. Soft budgets only notify and never reject requests. Configure them in routing settings.',
    dayBudget: zh ? '每日 Token 软预算' : 'Daily token soft budget', monthBudget: zh ? '每月 Token 软预算' : 'Monthly token soft budget',
    keyLimit: zh ? 'Key 并发上限' : 'Key concurrency limit',
    budgetHint: zh ? '预算填 0 表示不提醒；并发填 0 沿用全局上限。预算按已报告输入与输出合计，缓存和推理子计数不重复叠加。' : 'Zero disables a budget or inherits global concurrency. Budgets sum reported input and output without adding cache/reasoning subcounts again.',
    fairQueue: zh ? '按 Key 轮转安排入场队列' : 'Rotate admission queue by Key',
    fairHint: zh ? '同一 Key 内按先后顺序；不同 Key 轮流获得全局名额。最多等待 4096 个请求，目标实例仍单独检查容量。修改后保存生效。' : 'FIFO within each Key; Keys take turns receiving global permits. Up to 4096 requests may wait. Target capacity is checked separately. Save to apply.',
    active: zh ? '正在处理' : 'Active', queued: zh ? '正在排队' : 'Queued', limit: zh ? '上限' : 'Limit',
    inactive: zh ? '路由未运行' : 'Router is stopped', liveHint: zh ? '实时状态每 5 秒刷新；正在处理包含读取正文和响应传输，占用不等于引擎槽位。' : 'Live state refreshes every 5 seconds. Active includes request/response transfer and does not equal engine slot occupancy.',
    day: zh ? '今日' : 'Today', month: zh ? '本月' : 'This month', disabled: zh ? '未设置预算' : 'No budget set',
    exceeded: zh ? '已达预算' : 'Budget reached', near: zh ? '已用 80% 以上' : '80% or more used',
    incomplete: zh ? '存在部分或未知用量，已知用量可能低于实际消耗。' : 'Partial or unknown usage exists; known totals may understate consumption.',
    gap: zh ? '采集有待写入记录或曾出现中断／写入错误，预算读数可能不完整。' : 'Pending records, interrupted sessions or write errors may make budget readings incomplete.',
    noKeys: zh ? '在路由配置中添加 Key 后，可设置预算并查看对应并发。' : 'Add Keys in routing settings to configure budgets and view concurrency.',
    slow: zh ? '慢请求筛选' : 'Slow request filter', threshold: zh ? '至少耗时（毫秒）' : 'Minimum latency (ms)', all: zh ? '不过滤耗时' : 'Any latency',
    overview: zh ? '今日路由概览（UTC）' : 'Router today (UTC)', successRate: zh ? '成功率' : 'Success rate', coverage: zh ? '用量完整率' : 'Usage completeness',
    requestsToday: zh ? '今日请求' : 'Requests today',
    open: zh ? '查看路由' : 'Open router', updated: zh ? '更新于' : 'Updated',
    rejected: zh ? '拒绝' : 'Rejected', cancelled: zh ? '取消' : 'Cancelled', failed: zh ? '失败' : 'Failed', incompleteCalls: zh ? '未完成' : 'Incomplete',
  }
}
